type: BIGCONFIG_FILE

table_deployments:
  - collection:
      name: Snowflake Spend
      notification_channels:
        - email: operations@bigeye.com
        - slack: '#operations'
    deployments:
      - fq_table_name: PROD_WAREHOUSE.Bigeye Virtual Schema.SNOWFLAKE_SPEND
        columns:
          - column_name: APPROXIMATE_CREDITS_USED
            metrics:
              - saved_metric_id: daily_credits_per_user